import React from 'react';

const LoginSignupHeader = ({text}) => {
    return (
        <h2 class="text-center font-semibold text-gray-500 text-lg mb-4">
            {text}
        </h2>
    );
};

export default LoginSignupHeader;